apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
  labels:
    app: flask
spec:
  replicas: 1
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - name: flask-app
        image: yavuzkaramert/flask-app:080d7ea547f069df8109b49e4fb88be555f729ff
        imagePullPolicy: Always
        ports:
        - containerPort: 5000